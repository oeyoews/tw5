image: node:latest

pages:
  only:
    #- tags
    - tw6

  script:
    - yarn build@vercel

  artifacts:
    #enabled: false
    paths:
      - public
    expire_in: 20 minutes


  cache:
    paths:
      - node_modules
