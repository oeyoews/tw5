image: node:latest

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
  # note: this must global
  - npm i -g tiddlywiki
  - tiddlywiki --build index
  - rm -rf public/*
  - cp output/* public
  artifacts:
    paths:
    - public
  only:
  - main
