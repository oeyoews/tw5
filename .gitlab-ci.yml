image: node:latest

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
  - npm i -g tiddlywiki
  - tiddlywiki --build index
  - rm -rf public/*
  - mv output/* public
  artifacts:
    paths:
    - public
  only:
  - main
